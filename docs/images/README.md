# Screenshots pendentes

Esta pasta não contém nenhuma captura de tela real ainda. Para completar a documentação visual do projeto, produza e adicione aqui as seguintes imagens (formato `.png`, largura sugerida de 1280px):

- `home.png` — Tela inicial (`/`), com o título "DSVendas" e o botão "Acessar dashboard".
- `dashboard.png` — Tela do dashboard (`/dashboard`) completa, mostrando o gráfico de barras de taxa de sucesso, o gráfico de rosca de vendas por vendedor e a tabela paginada.
- `dashboard-mobile.png` (opcional) — Dashboard em viewport mobile, para demonstrar responsividade.

Depois de adicionar os arquivos, referencie-os no `README.md` da raiz do projeto, por exemplo:

```markdown
![Dashboard de vendas](docs/images/dashboard.png)
```

Nenhuma imagem deve ser gerada artificialmente — as capturas devem vir da aplicação rodando de verdade (backend + frontend localmente).
